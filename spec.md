# 计算机网络 AI 助教项目骨架 Spec

> 状态：v1.0，已审批（2026-07-28）  
> 技术基线选择：方案 A（前后端分离，具体实现写入 `plan.md`）

## 背景

仓库当前为空。项目需要在三周内由五名成员协作完成一个独立 Web 应用原型，覆盖课程资料处理、知识库与评测、RAG 问答、教学任务引擎和界面展示。

同济 Canvas 当前不提供本项目所需的 AI 能力，团队也没有 Canvas API、LTI 或课程资源导出权限。因此，本仓库只承载独立系统，不依赖 Canvas 的身份、资源、成绩或学习行为接口。

本次工作不是提前完成各成员的业务实现，而是建立所有人可以并行开发的共同基线，先固定模块边界、数据契约、运行方式和验收入口。

## 目标

- 建立可本地运行的独立 Web 应用骨架。
- 为 A–E 五个角色提供边界清晰、可独立开发的模块。
- 将课程资料、问答、引用、任务和评测所需的共享数据契约固化为单一事实来源。
- 提供不依赖真实模型、向量数据库或付费服务的最小端到端演示。
- 建立统一的配置、测试、代码质量、接口变更和协作规范。

## 功能需求

- F1：提供前端、后端和共享契约之间边界明确的工程骨架，各部分可以独立开发和验证。
- F2：开发者完成一次环境准备后，可以按文档启动最小系统，并从界面观察后端服务状态。
- F3：提供独立系统内部的课程资料导入契约和模拟实现，能返回资源标识、处理状态和结构化来源信息。
- F4：提供课程问答契约和模拟检索链路，返回回答、置信信息和至少一条可定位到资料来源的引用。
- F5：提供教学任务契约和模拟流程，支持读取任务列表、查看任务详情以及表达任务状态；结构应能承载六类教学任务。
- F6：提供独立系统内部的学习行为事件契约，用于记录任务或问答交互；不实现 Canvas 成绩或行为回写。
- F7：统一定义并校验 Chunk Metadata、知识点、引用、评测条目和任务模板等共享数据结构。
- F8：为资料处理、问答与评测、RAG 与模型、任务引擎与产品、平台与集成分别保留清晰的模块入口，避免成员直接依赖他人模块内部实现。
- F9：提供最小模拟数据，使“打开界面 → 提问 → 获得带引用回答”和“查看任务 → 查看任务详情”两条链路可以离线演示。
- F10：为模型、Embedding、Reranker 和向量存储提供可替换的适配边界；骨架默认使用离线模拟实现。
- F11：提供开发入门、分支命名、提交信息、接口变更和验证方式说明，接口变更须先更新共享契约。
- F12：提供自动化质量入口，至少覆盖格式检查、静态检查、单元测试和构建检查。

## 非功能需求

- N1：首次接入成员应能依据仓库文档在常见开发环境中完成启动，不依赖个人机器上的隐式配置。
- N2：仓库不得包含密钥、令牌或个人路径；所有必要配置均提供无敏感信息的示例。
- N3：跨模块请求、响应和共享数据必须可以自动校验，禁止前后端分别维护互相漂移的字段定义。
- N4：后端错误应返回稳定的错误结构；日志应足以定位请求和模块，但不得记录密钥或完整隐私数据。
- N5：默认测试和最小演示必须可离线、可重复执行，不消耗付费模型或云服务额度。
- N6：模块命名、接口字段和状态枚举保持一致；未确认能力不得伪装成已实现。
- N7：骨架应支持五人通过功能分支并行开发，单个角色的正常改动不应要求重写其他角色模块。

## 不做的事情

- 不接入 Canvas API、LTI、课程身份、课程资源同步、成绩回写或学习行为回写。
- 不在骨架阶段确定最终向量数据库、RAG 框架、Embedding、Reranker 或大模型供应商。
- 不实现生产级登录、权限管理、多租户、计费、审计后台或高可用部署。
- 不完成 PDF、PPT、字幕、公式和协议图的正式解析流水线。
- 不完成真实向量检索、模型生成、LoRA 微调或评测调参。
- 不代替 B、C、D、E 完成课程语料、30 条问答、15–20 条评测集或六类任务的最终内容。
- 不把模拟接口描述为已经具备的 Canvas 集成能力。

## 验收标准

- AC1（F1、F8、N7）：仓库结构能明确指出每个模块的职责、公共契约和禁止跨越的边界。
- AC2（F2、N1）：新成员按照文档完成环境准备后，能够启动前后端并在界面看到服务正常状态。
- AC3（F3、F7）：使用示例资料调用导入契约，能得到符合校验规则的资源标识、状态和来源 Metadata。
- AC4（F4、F7、F9）：输入示例问题后，界面能展示模拟回答及至少一条包含资源、章节和页码信息的引用。
- AC5（F5、F9）：界面能展示模拟任务列表和任务详情，任务结构包含知识点、资料、完成判据和 AI 反馈介入点。
- AC6（F6）：示例交互能够形成符合统一契约的学习行为事件，且不存在 Canvas 回写调用。
- AC7（F7）：合法的 Chunk、知识点、引用、评测和任务样例通过自动校验；缺少必填字段的样例被拒绝。
- AC8（F10、N5）：不配置真实模型或向量数据库时，最小端到端链路和自动化测试仍可运行。
- AC9（F11）：仓库文档明确说明启动、测试、分支、提交、接口变更和角色协作规则。
- AC10（F12、N2、N4、N5）：统一质量命令能够完成格式、静态检查、测试和构建，且仓库扫描不到真实密钥。
- AC11（项目边界）：从全新环境运行骨架时，不需要 Canvas 账号、Canvas 网络访问或学校平台接口权限。
