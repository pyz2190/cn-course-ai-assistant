# 计算机网络 AI 助教项目骨架 Checklist

> 状态：v1.0，已审批（2026-07-28）  
> 验收完成：`npm run check` 全部通过；API 9 项测试、Web 8 项测试通过，
> 契约无漂移，生产构建成功，`npm audit` 为 0 漏洞（2026-07-28）。

## 实现完整性

- [x] API 健康接口可调用并返回稳定状态。（验证：API 路由测试）
- [x] 资料导入接口返回资源标识、状态和完整 Metadata。（验证：API 路由测试）
- [x] 问答接口返回回答、置信度、请求 ID 和至少一条引用。（验证：API 路由测试）
- [x] 引用包含资源、章节、页码和摘录。（验证：API 与 Web 组件测试）
- [x] 六类任务均可从任务接口读取。（验证：任务类型集合测试）
- [x] 任务详情包含知识点、资料、完成判据和 AI 反馈介入点。（验证：API 与 Web 组件测试）
- [x] 学习行为事件可被独立系统接收并返回已记录事件。（验证：API 路由测试）
- [x] 模型、检索和存储均由端口隔离，默认使用 Mock。（验证：无外部配置运行测试）

## 契约

- [x] Chunk Metadata 包含接口表规定的最小字段。（验证：Schema 与 Fixture）
- [x] 知识点、引用、评测条目和任务模板均有 JSON Schema。（验证：`contracts/schemas/`）
- [x] 非法页码、空问题、越界置信度或缺少必填字段会被拒绝。（验证：模型与 API 错误测试）
- [x] Web 类型由 OpenAPI 生成，不存在独立手写的重复 DTO。（验证：`npm run contracts`）
- [x] 重新生成契约不会产生未提交差异。（验证：`npm run check:contracts`）

## Web

- [x] 页面能展示 API 在线或离线状态。（验证：`SystemStatus` 组件测试）
- [x] 提问过程有提交、加载、错误和成功状态。（验证：`ChatPanel` 组件测试）
- [x] 回答引用可见且具有资源定位信息。（验证：引用组件测试）
- [x] 任务列表和详情可切换。（验证：`TaskWorkspace` 交互测试）
- [x] 页面明确标记当前使用模拟数据且不依赖 Canvas。（验证：页面文案）

## 协作与安全

- [x] README 覆盖安装、启动、检查和目录说明。（验证：全新虚拟环境安装与检查）
- [x] CONTRIBUTING 覆盖 `feat/<role>-<topic>` 和提交前缀规范。（验证：文档检查）
- [x] 接口变更文档说明“先改契约，再改实现”。（验证：`docs/interface-change.md`）
- [x] A–E 五个角色均有明确目录边界。（验证：`docs/roles.md`）
- [x] `.env.example` 不含真实密钥，仓库中无令牌或个人绝对路径。（验证：敏感信息扫描）
- [x] 系统不包含 Canvas API、LTI、成绩或学习行为回写调用。（验证：运行源码扫描）

## 编译与测试

- [x] Ruff 格式与静态检查通过。（验证：`npm run lint:api`）
- [x] API 单元与路由测试通过。（验证：pytest，9 项）
- [x] ESLint 与 TypeScript 检查通过。（验证：Web lint 与 typecheck）
- [x] Web 组件测试通过。（验证：Vitest，8 项）
- [x] Web 生产构建通过。（验证：Vite build）
- [x] 根目录统一检查命令通过。（验证：`npm run check`）
- [x] CI 配置执行与本地等价检查。（验证：workflow 调用同一根检查命令）

## 端到端场景

- [x] 场景 1：API 问答测试与 Web 问答测试验证带章节和页码引用的模拟回答。
- [x] 场景 2：API 验证六类任务，Web 交互测试验证任务详情切换及完整字段。
- [x] 场景 3：未配置模型、向量数据库或 Canvas 凭据，完整检查和最小演示可运行。
- [x] 场景 4：API 验证统一错误结构，Web 组件测试验证可理解的错误提示。
